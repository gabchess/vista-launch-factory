# Vista Launch Factory

Root stays thin. Pull deeper files when the phase needs them.

1. Board truth: GitHub Issues on this repo — exactly one `ready-for-agent` at a time in Ralph cadence.
2. Forge-only push/gh mutate; Gabe merges. No auto-publish. No invent Vista pricing.
3. Domain language: `docs/CONTEXT.md`. Claims Lock before adapters. Barry HITL on copy/creative.
4. Current scope: `docs/adr/0017-specialist-routing-and-current-scope.md` supersedes the historical Quorum Desk / blanket HOLD defaults for later user-authorized work. Keep historical fixture decisions intact.
5. Specialist entry: `codex/launch-factory/SKILL.md` routes the selected stage through `engine/specialists/`. Specialists recommend; actual Gabe or Barry decisions remain human. No legacy boolean as approval authority.

## Agent skills

### Issue tracker

GitHub Issues on `gabchess/vista-launch-factory` via `gh`. See `docs/agents/issue-tracker.md`.

### Triage labels

Defaults: `needs-triage` · `needs-info` · `ready-for-agent` · `ready-for-human` · `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: glossary at `docs/CONTEXT.md`; ADRs under `docs/adr/` when present. See `docs/agents/domain.md`.
