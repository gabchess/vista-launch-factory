# Login animation

Define the viewing context, dimensions, duration, loop seam, first/last frame and motion hierarchy. Use approved UI states and legible product text. Keep essential meaning in static content and offer a reduced-motion treatment. Review actual rendered frames, transitions and playback. A composition file or successful CLI exit alone is not visual QA.

For an approved finishing revision, read [short motion finishing](../finishing/SKILL.md). Keep the accepted story, set the requested root duration, select two to four motion rules and write an exact music/SFX cue sheet. Kinetic headlines remain authored text. Caption JSON applies when real speech exists. A silent or music-only edit does not require narration. Label the actual source and render providers separately.

Return concrete draft material or a specific review finding. Mark missing source, tool access, and uninspected output as gaps. Use only the selected product and voice context. Every new rendition needs its own version, hash, and human review.
