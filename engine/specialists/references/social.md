# Written social

Choose one claim and a distinct angle for each placement. A short post should stand on its own; a thread needs a logical sequence with new information in each entry. Preserve qualifiers through compression. Verify current platform limits before final export rather than embedding a permanent character limit in this skill.

Return concrete draft material or a specific review finding. Mark missing source, tool access, and uninspected output as gaps. Use only the selected product and voice context. Every new rendition needs its own version, hash, and human review.
