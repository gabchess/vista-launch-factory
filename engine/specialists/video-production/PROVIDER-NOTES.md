# Provider notes and verification boundary

Recorded 8 September 2026. Recheck current tool schemas and official documentation on the recipient's account before a paid job. The initial route observations came from read-only tool discovery in the author's desktop session. Later production lessons are labelled separately below. Neither establishes portable worker credentials.

## Discover the account workflow before primitive calls

Before planning generation, import or edit calls, discover and read the relevant workflow in the recipient's current ChatCut account through its available workflow catalog, such as `manage_skill`. Match the workflow to the film's task and keep its returned identity in the run record. Reuse the existing project and accepted assets, applying the instructions to the remaining work while preserving the authorized review schedule.

Name the selected orchestration surface. If ChatCut's native Agent mode is chosen, attach the relevant workflow there and invoke that surface through its supported controls. If an external agent drives tools through MCP, report external MCP orchestration. Fetching or loading workflow instructions supplies context; it does not start the web agent.

In the recorded 8 September 2026 session, account discovery returned 51 workflows, and the operator read AI Cinematic Short Film and Seedance 2.5 TVC Director. The film was assembled by the external agent through MCP following the fetched cinematic instructions. Native web-agent execution remained unverified. Keep workflow discovery, attachment and execution as separate observations.

## Reuse the actual template source

When a brief asks for a named template, inspect its source files before building. Record the source URL, revision or hash, entry file, license, retained components and requested changes. Reuse its working interaction or composition code where permitted. Record a widescreen or layout redesign as an explicit adaptation. A screenshot reference supports visual direction; it does not establish source reuse.

Check that a fetched workflow includes the template files or materializer it names. If those files are absent, record the gap and identify the source used instead. Keep template authorship, fetched workflow instructions, executing tool, media producer and final editor separate in the receipt. A HyperFrames source derivative can be edited in ChatCut while retaining both attributions. Report each executed provider action only from its returned result.

## Revise an authored app scene

Render HTML, vector UI and type at the requested native dimensions. For an actor-quality request, inspect the best accepted source first and enhance only the needed picture through an authorized provider. Keep original dialogue beneath silent enhanced picture, with an offset map, hashes and a before/after audio comparison when claiming preservation. Check faces, hands, background continuity and cut boundaries. Export dimensions alone leave source-detail improvement unproven.

Use the supplied transparent vector or verified alpha asset for an agent avatar. Inspect its edges on the final background. Keep one active app view with an anchored composer and a readable context pane when that is the approved layout. Remove obsolete phone or screen layers after a layout change; hiding them behind a new layer can leave stray edges in motion.

For requested real listings, bind each visible card to a fetched listing URL, retrieval date, seller-posted model, asking price, location and actual listing photograph. Retain the image source and hash with its rights status. Confirm that the picture belongs to that listing; a plausible product image cannot substitute silently. Distinguish a seller's asking price from a verified bargain or market-value claim.

Follow the latest approved product-vision wording and visible-copy amendment, including an explicit request to remove draft overlays. Keep the intended future behavior and unexecuted actions clear in production and presentation notes. An authored sign-in, wallet import, payment state or search result does not prove that action ran. Preserve the exact approved CTA and the current human review scope. Do not add generic warnings back into the film after their removal was explicitly approved.

## Current candidate route

ChatCut-native actor takes, authored HyperFrames UI and a ChatCut edit/mix are a candidate workflow. `list_projects` responded through an authenticated desktop connection. `submit_video` was inspected as a one-asset asynchronous library job; `track_progress` observes it, and `edit_item` is needed to place it in an edit. Project creation returns an active timeline and video track. For automatic ducking, `edit_track` uses the actual speech-bearing track as anchor and music as follower. Leave manual duck depth unset unless the brief calls for it. No generation result was tested by these observations.

The exposed model choices included `seedance-2-5`, `seedance2`, `seedance2fast`, `seedance2mini`, `kling`, `minimax-h3`, `minimax-h3-max` and `omni`; reference arrays distinguish images, video and audio. This is a dated inventory, not a default selection or a promise that a specific voice/identity control works. Check the current required fields, duration choices, quote and audio controls before composing a tool call. Do not infer a backend API key from desktop MCP authentication.

Hosted ChatCut generation consumes ChatCut credits; reasoning through an external agent uses that host's allowance. A plan label or working MCP connection does not prove free generation. Source: [ChatCut credits policy](https://chatcut.io/docs/credits-policy). Record the quote's unit. If only credits are known, the USD budget check stays held until an evidenced conversion is available.

Higgsfield's read-only model recommender returned native-audio candidates in the author's session. That is a recommendation, with no local identity, voice, cost or quality proof. A switch requires a new provider-specific reference check, quote and authorized sample. HeyGen is another own-account option whose actor/runtime path was not tested by this package. No automatic fallback follows a rejected sample.

A later HeyGen read-only style request returned `oauth_token_invalid_grant` and required reauthentication, so that route was unavailable in the author's session. ChatCut voice catalog and TTS feature discovery returned usable selection records, without generating speech or proving a video balance/quote. Keep actual IDs and the recipient's cast choices in the release workspace. Seedance's inspected guidance requires audio and separates image, video and audio references; prompt for clean dialogue or intended silence and inspect what it returned. Do not encode a generic audio-off flag that the selected model does not support.

For the inspected Seedance 2.5 adapter, put duration, ratio, resolution, fps and model in the supported tool parameters, outside the prose prompt. Its reference arrays and first/last-frame controls are mutually exclusive; select the supported reference-array mode when using approved voice references. Verify that restriction against the current schema before submission. The generic prompt template labels adapter control data so it can be removed from the actual prompt where required.

## Later picture-edit lesson

A targeted generative edit can fix a foreground detail while changing background people or props. Compare the original and corrected picture across the whole scene, including continuity and areas outside the requested edit. Show those changes at the rendition review; a successful local fix does not complete creative approval.

When dialogue is already accepted, keep the original source as the sole dialogue track and place the corrected picture on a separate muted video track. In an observed ChatCut edit, creating a track with `muted: true` returned `muted: false`; an explicit mute update followed by readback was required. Verify the actual track state before export instead of relying on the creation request.

To claim unchanged audio, compare both exports' decoded PCM at the same sample rate, channel layout and sample format, and record the byte counts and hashes against the exact media versions. Matching audio confirms preservation within that comparison. It does not certify the revised picture or replace its human review.

## Editorial acceptance and generation-reference eligibility

Keep the accepted composed master as the editorial deliverable. Human acceptance of its picture and audio does not establish a generation provider's reference eligibility. The inspected Seedance 2.5 guidance permits eligible, unmodified ModelArk outputs from the same account within the provider's trust window. Retain those original provider assets and provenance when preparing a supported reference route. Bind human approval to each reference's named role, such as one actor's identity and voice or another actor's appearance; exclude unrelated or rejected details explicitly.

In one observed attempt, an imported, human-approved composition combining corrected picture with accepted original audio failed with `InputVideoSensitiveContentDetected.PrivacyInformation` / `input_video_real_person`. It returned no output, and the billed charge was unknown. Preserve that failed receipt and unknown spend. Do not strip metadata or re-upload the rejected bytes to evade the check. A filename, local hash, successful import or editorial approval does not establish provider trust or entitlement.

A separate submission using the original unmodified Seedance assets completed successfully on 8 September 2026. The provider accepted those same-account references for that request. This observation does not establish acceptance of every original output or extend a trust window. The billed amount was not returned, and visual/audio QA and human review of the new output remained pending. Preserve role-specific human decisions and review every new output. If a later request is rejected, retain the failure and resolve an eligible reference or stop; do not loop through disguised copies of the same input.

## Prompt classification and adaptive ratio

State whether a video reference supplies identity, voice or scene context, or asks the provider to continue existing footage. Seedance 2.5 can infer edit or extension intent from the prompt even when the submitted `taskMode` is `generate`. Check the current provider guide for classification cues and the controls required by the inferred task. Keep approved creative words and reference roles intact when diagnosing a parameter rejection.

An observed request failed with `InvalidParameter.TaskTypeConstraint` because the inferred extension required `ratio: "adaptive"`. It returned no asset and its billed charge was unknown. Preserve the failed receipt. When the documented correction fits the authorized scope, prepare one separate job that changes the rejected parameter, links the failure and records its own spend. Keep other approved inputs stable so the effect of that correction can be assessed. Repeated submission of the unchanged rejected request remains unsupported.

The tool's "match adaptive" hint leaves the output geometry unresolved. Inspect the returned media's width, height, duration and frame rate before timeline placement. Keep the approved 16:9 canvas when the returned proportions confirm it, and check that the usable source interval fits the edit slot. Resolve a geometry or duration mismatch before placement.

In the 8 September 2026 observation, changing only the generation ratio from `16:9` to `adaptive` produced a new 1280 × 720 asset lasting 4.042 seconds at 24 fps with embedded dialogue. The four-second request, original unmodified reference and creative words stayed unchanged. The generation record retained `aspectRatio: adaptive` and `functionMode: omni_reference`. This confirms that specific correction and the retained 16:9 proportions. Visual/audio QA and human review remained pending at that checkpoint; the billed amount was unknown. Apply the same evidence checks to each later job.

## Authored product picture with generated dialogue

For an approved phone or product insert, a generated actor take may supply the organic spoken performance while authored HyperFrames picture covers the actor. Build readable UI and prices from approved source claims or concept data whose status is recorded in the brief and handoff. Visible copy follows the approved amendment described above. Generated product text cannot establish product behavior or pricing. Keep the source take's producing service, model, job and asset lineage even when its picture is covered in the final edit.

Retain the original source bytes and SHA-256, the derived audio file and SHA-256, and the audio extraction settings. Record the provider source duration, export timing, measured speech interval, selected trim and timeline offset. Keep the picture project's version and source bindings with the composition. Preserve editable clip, music and SFX tracks with cue times, source/provider attribution, license status and media hashes. Label room tone embedded in the speech source; only claim separate stems when those files exist.

ASR can normalize a spoken amount such as "nine hundred and fifty dollars" to `$950`. Save its raw transcript and parsed value alongside the locked spoken line. The amount check can pass while the exact verbal expansion remains unverified. Human listening must confirm the full approved words and voice continuity on the exact rendition. Keep speech captions tied to the approved transcript and measured audio timing, with one authoritative caption source for the selected editor.

At a recorded 8 September 2026 checkpoint, one ten-second ChatCut/Seedance 2.5 native source from an accepted original reference had been generated and exported. Source decode passed, and the exported media and derived audio hashes were retained. ASR returned a normalized dollar amount; exact spoken expansion still required listening. Authored picture and composition were in progress, with final export and human acceptance pending. Preserve source QA and composition review as separate records, and keep the new dialogue attributed to its actual producer. Unchanged approved story and reference decisions continue to apply to their original scope.

## Time UI reveals to the spoken meaning

Once approved dialogue exists, derive UI reveals for spoken facts and actions from its measured phrase windows. Storyboard timings stay provisional until checked against that audio. For a price or action phrase, inspect adjacent rendered frames around the phrase onset and the reveal's settled state, then verify a readable hold. Move related tap cues with the UI and keep the accepted voice file untouched.

Build one event map from the final animation, with stable event IDs, source and timeline times, UI actions, speech windows and linked sound cues. Derive SFX from that map and replace obsolete clicks after edits. Keep cue media, source/provider, rights status and hashes in the editable handoff. Caption only the selected audible dialogue tracks: retain their source hashes and trim offsets, align the locked transcript to actual speech, and exclude music, SFX and ambience from caption input. Inspect caption text after generation so ASR on those tracks cannot introduce invented dialogue.

A matching transcript and a valid layout leave audiovisual sync unverified. Carry prior visual and voice acceptance for unchanged components under the revision fast path below. Scope the new review to timing on the exact rendered rendition; a source timing correction remains pending until that render has been checked and reviewed.

Set an insert's end from the measured speech end and the brief's required reading or reaction hold. Inspect the cut around the last word so the trim preserves its full audible tail. Keep enough time to read the revealed result, then remove unused storyboard time within the current brief or an explicit trim amendment. Record the changed source interval and inspect its transition into the next shot. A fixed storyboard duration alone does not justify a silent tail.

When the human explicitly authorizes finishing the remaining approved story in one pass, follow the [continuous-production instruction](PROTOCOL.md#explicitly-authorized-continuous-production). Provider jobs and automated checks run inside that pass, with one assembled review for takes still awaiting acceptance. Preserve each job's provenance and spend record; settled story and role samples continue to apply within their approved scope.

## Fast path for specified revisions

For a requested non-generative revision, reuse the current editable project and existing media with approved sound. Change only the specified elements. An explicit request to remove visible draft labels can amend the brief; record that instruction against the changed scope and retain honest producer provenance and concept/roadmap status in the production and handoff records.

Run the required framework check once, render locally and inspect the affected frames. Repeat checks only when a failure or further edit requires them. Hashes identify reused audio inputs; a claim that delivered audio is unchanged requires matching the exports' decoded PCM at the same sample rate, channel layout and format. Keep the comparison receipt bound to both media hashes. Skip ASR reruns and broad research for unchanged audio.

Deliver the verified local render directly for review. Use an additional cloud render only when it is needed to reproduce editor-specific effects. Synchronize the revised source asset in the existing ChatCut edit and read back the selected asset and timeline binding so later assembly uses the delivered version.

Keep prior decisions for unchanged components bound to their existing hashes. Scope the new human review to the requested edits and bind its result to the new rendition hash. Continue settled story and cast decisions without another interview or actor generation. This fast path supplies a procedure; each execution still needs its own check and review receipts.

## Recipient preflight

For separately requested speech, the [direct ElevenLabs adapter](ELEVENLABS-DIRECT.md) uses the recipient's own account. It selects the voice by role and keeps the inference.sh wrapper as an explicitly selected alternative billing route. Organic actor audio already approved for a sample remains that sample's source. No new story or cast approval is required for unchanged accepted inputs.

Load the installed host's current provider skill and inspect callable controls. Check recipient account access without exposing secrets. Resolve identity and voice references for that provider, including whether separate speech audio or only an embedded mix is returned. Verify duration and trim support, exact speech behavior and a current quote. Hold any required unsupported control instead of inventing arguments or promising continuity.

The first authorized output must prove the chosen path. Record actual producer, model, gateway and provider IDs, inspect returned bytes, listen to the exact line and show the rendition to the human. Keep install, discovery, invocation, authenticated tool access, first output and acceptance as separate statuses in the handoff. These notes contain no account IDs, tokens, paid prompt text or live media dependencies.
