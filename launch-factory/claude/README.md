# Claude host package

- **ZIP:** `launch-factory-v0.1.0.zip` (one-root `launch-factory/`)
- **Install:** see `docs/INSTALL-CLAUDE.md`
- Payload mirrors `codex/launch-factory` for dual-host parity intent.
- Live Claude activation not freshly verified — see `HOST-MATRIX.md`.
