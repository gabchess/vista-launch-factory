# Claude host door

**A1 stub.** The installable archive `launch-factory-v0.1.0.zip` arrives in **A2**.

Until then:

- Dual-host layout is reserved here so Codex and Claude doors stay parallel.
- Do not claim Claude package parity is verified ([HOST-MATRIX.md](../HOST-MATRIX.md)).
- Install guidance: [docs/INSTALL-CLAUDE.md](../docs/INSTALL-CLAUDE.md).
- Codex skill is available now under `../codex/launch-factory/`.

When A2 lands, this directory should contain `launch-factory-v0.1.0.zip` as a one-root Claude package with the same Claims Lock / Barry / HOLD honesty as Codex.
