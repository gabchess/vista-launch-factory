# Vista Launch Factory local demo loop, Phase A

Status on 9 September 2026: authorized for local Phase A execution. The planning snapshot below remains part of the record.

Planning snapshot: this document began as a proposed execution plan. Writing it did not start a loop, new generation or deployment.

## Execution addendum, 9 September 2026

Gabe authorized the full local Phase A loop and overnight execution. D00 starts from audit candidate `627c93fb23ae3a1ecd1cdb979396c8d23459b295` in the isolated `nova/vista-demo-local` worktree. PR #41 remains open and unmerged. Gabe retains merge authority.

An AgentsKit marketing reviewer may make provisional review decisions at demo review gates during this run. Record each outcome as a delegated agent review with the artifact version, hash, review context and reviewer route. This delegation applies to the local demo run on 9 September 2026. It does not create a Gabe review event, a Barry decision or authenticated human approval. Barry's final copy and creative approval remains pending.

The run remains local. It may prepare review and handoff artifacts. It may not send, publish, deploy or represent a provisional decision as client acceptance. Missing private human and UI footage remains a declared input gap. The budget balance must be reconciled before any paid generation. D00 makes no paid provider calls.

## Current client direction

Reggie's latest messages, supplied directly by Gabe in this session, accept local execution for this test project. He described organic video as low production, usually edited from supplied human and UI footage. The meeting remains September 9. Follow the calendar invite for its time; do not infer the timezone from the quoted messages.

The deadline target is a retained, supervised local run through six outputs and a campaign, its full-run recording, and an engineering roadmap. Build the hosted non-engineer product after the presentation using [Phase B](2026-09-09-vista-operational-loop.md).

Source accuracy, actual artifacts, review/revision and the recording still matter tomorrow. A new Replit deployment, durable service, second non-engineer and recipient-cloud install are Phase B gates. Reggie's concession does not supply Barry's editorial approval or validate a missing output.

## Selected demo

Use the Vista Work public announcement and linked-task guides in the [source table](../specs/2026-09-09-vista-operational-loop-design.md#source-and-audience-decision). This public-source run addresses the problem Reggie described: finding the caption, creative and feedback across separate tools. The audience is his in-house social media team.

Tix remains the backup package and a supplementary creative example. Preserve its approved masters and source identity. Never fill a missing Vista slot with a renamed Tix file.

Record the actual local Codex/agent session, followed by the real review page. A page that displays saved content is the review page. Do not build a chat facade that appears to generate while a hidden person performs the steps elsewhere.

## Execution approach

The local orchestrator reads the existing Launch Factory protocols, retrieves public evidence, runs the selected specialists and records their actual results. MCP tools can edit/render media where available. The operator retains source and job receipts and pauses at human gates. After interruption, resume from that record.

Use one working route per output. Reuse original templates, video recipes and source validators. Prefer cuts of supplied raw footage; otherwise use a declared documentary sequence built from public product imagery, narration and captions. No new private human/UI footage has been supplied for this run. Do not invent it or describe an authored scene as an observed account recording.

Do not spend this window generating restaurant actors, recreating ChatCut, integrating extra providers or building the hosted runtime. The richer Tix film can illustrate a separate capability.

The existing $500 ceiling covers prior and new work. Reconcile receipts before paid generation. Record local model work as local-harness execution; do not invent an external request ID or a zero-dollar bill. Prefer direct ElevenLabs for narration when needed, with a voice appropriate to the speaker. Gabe's clone is only for Gabe.

## Local run records

Create `packages/camp_vista_work_public_001/` without changing accepted Tix artifacts. Keep confidential source/provider detail outside the distributable package where needed.

| Record | Required content |
| --- | --- |
| `RUN.md`, `run.json` | Local-assisted mode, product/run ID, source digest, operator, start/end times, actual routes and pending steps. |
| `sources/`, `source-manifest.json` | Captured URLs/bytes, retrieval times, hashes and announcement-date meaning. No invented private Loom or deployment date. |
| `claims/`, `voice/`, `requests/` | Selected claim spans, original voice analysis, Barry seed reference and actual specialist requests. |
| `events.jsonl`, `jobs/`, `costs.json` | Observed generation, retry/reuse and provider events. Unknown result or spend remains unknown. |
| `artifacts/` | Blog, five emails, changelog, social video, login animation, popup graphic/copy and campaign. |
| `reviews/` | Version/hash/context, actual human feedback and its local-session origin. Gabe is Gabe; Barry may remain pending. |
| `review.html`, `export-manifest.json` | Actual artifacts, source context, remaining decisions and matching file hashes. |
| `recording.json` | Raw recording location/hash, presentation cut and time-jump notes. |

These are local evidence records. Browser notes remain intent, and local conversation records do not pretend to be a deployed identity service. The call's output is a review package. Nothing sends or publishes.

## D00. Start from the audit fix

**Owner:** Forge prepares; Nova verifies. **Scope:** audit candidate, current plan and run metadata.

- [ ] Use candidate `627c93fb23ae3a1ecd1cdb979396c8d23459b295` or a reviewed descendant in an isolated worktree. Preserve the dependency on unmerged PR #41. Gabe owns PR merges.
- [ ] Reproduce article/package/entrypoint checks and confirm default verification is read-only. The audit's reported results are prior evidence until rerun here.
- [ ] Record the accepted local-test scope, missing private footage and remaining budget. Prepare the screen recorder before the retained source-to-package run begins.
- [ ] Reserve the final recording/review window before optional polish.

Acceptance: exact code and input mode are identified, the portability fix is present, and the local run can start without cloud setup.

## D01. Capture the public source and select its voice

**Owner:** evidence specialist; verifier: Nova. **Scope:** new campaign source, claim, voice and request files.

- [ ] Retrieve the announcement, linked-task guide and getting-started guide. Save source identity and hashes before drafting.
- [ ] Select the announced change, Work access path and Reggie's audience. Omit conflicting plan eligibility and unsupported performance figures.
- [ ] Include the existing Barry email seed as one attributed voice sample. Retain its provenance limit alongside original Vista channel analysis.
- [ ] Define all five email segments and distinct actions. Leads learn through the public article; customers use login and Work. Unknown CRM mapping stays unconfigured without blocking draft copy.
- [ ] Run canonical source/route checks in the new workspace. Show the brief, source selection and missing context for Gabe's confirmation.

Acceptance: one confirmed source/voice context feeds every output. No Tix claim enters the Vista campaign.

## D02. Generate writing, campaign and media brief locally

**Owner:** existing channel specialists; a non-author reviews their work. **Scope:** the new campaign and narrowly needed original prompt corrections.

- [ ] Invoke the actual protocols in the local harness. Record delegated versus inline execution accurately.
- [ ] Produce the release-led blog, exactly five distinct emails, specific changelog, popup copy and actual written social in files.
- [ ] Build the proposed weekly calendar with audience, action, owner, dependency and review state per placement. Missing media keeps its placement pending.
- [ ] Create a concise social edit plan and login-animation plan from the same evidence. Declare supplied footage or the public-imagery alternative, final audio mode, captions, CTA and route.
- [ ] Run structural/source checks and independent semantic/voice review. Show the scripts and storyboards to Gabe before paid media work.

Acceptance: all writing is complete and traceable, and the actual human has reviewed the new media brief. A model cannot approve it.

## D03. Produce simple media and receiving examples

**Owner:** video/design specialists; verifier: non-author media/browser reviewer, then Gabe. **Scope:** source-specific media, popup graphic and small host examples.

- [ ] Use the existing validated MCP/skill route to make a basic social edit. Generate no optional actors. For narration, derive caption timing from the final audio and remove dead air.
- [ ] Include restrained music/SFX where appropriate; dialogue remains clear. Retain usage records.
- [ ] Render the short login animation from an original template. Produce a poster and embed example with muted looping, `playsinline`, useful fit and reduced-motion fallback. A new login app is unnecessary.
- [ ] Produce the popup graphic and copy preview. Check keyboard close, focus return, narrow layout and CTA.
- [ ] Decode the actual media. Watch/listen to the whole social export and inspect mobile captions. Inspect two animation loops and fallback. Save exact hashes and return every new video to Gabe.

Acceptance: all required media are real and source-bound. Unknown provider results are reconciled before retry. Tix masters remain untouched.

## D04. Assemble the review and take a real revision

**Owner:** local package builder; verifier: a different reviewer. **Scope:** new campaign review and manifest. If needed, add a small `engine/local_run/build_review.py` accepting `--workspace`; do not build a service.

- [ ] Reuse the current full-content review pattern. Generalize only the input binding needed for this product, avoiding Tix labels and builder paths.
- [ ] Verify all six categories, every email, popup graphic and media hashes. A storyboard or missing-media placeholder cannot close a slot.
- [ ] Show source context, version and actual review state beside each artifact. Preserve pending Barry decisions.
- [ ] Take one real, focused revision from Gabe. Rerun that asset and affected placements, preserve v1 and return v2. Do not invent feedback for the recording.
- [ ] Close/reopen the local review and resume from run files, checking source/artifact bindings again. Record this as supervised local continuation. Service recovery stays in Phase B.

Acceptance: a complete same-source review package and an actual targeted revision exist. Default verification is read-only and complete-folder relocation retains valid bindings.

## D05. Keep the recording and package the handoff

**Owner:** Gabe records, Nova verifies, Forge packages. **Scope:** recording, run bundle, handoff updates and commit-specific export.

- [ ] Record the accepted local source-to-package run. If rehearsal was needed, retain the later real run; never present opening prebuilt content as new generation.
- [ ] Match source digest, run ID, events, human decisions and artifact hashes to the raw recording. Identify deliberate reuse.
- [ ] Save the raw capture before making a short presentation cut. Mark time jumps over generation waits. Product videos remain outputs, not the full-run recording.
- [ ] Update current handoff/human-gap docs. Explain supervised local operation and the subsequent app work. Describe n8n exports as preparation where that is all they perform.
- [ ] Build the new ZIP/quickstart from the reviewed commit. Verify archive entries and open the local review from a fresh extracted directory, with no symlink to the builder's engine.

Acceptance: execution evidence, six reviewable output categories, a campaign, real revision and recorded continuation support the demonstration. The roadmap identifies remaining non-engineer operation.

## Deadline and stop rules

Prepare recording before D01 and capture the retained run as it happens. Complete D01–D04 before a deck or app facade. Reserve the final 90 minutes before the confirmed call for recording checks, the edited walkthrough and rehearsal. This is an internal allocation, not a client requirement.

If the new run cannot finish in time, preserve its exact progress and failure. Present the existing Tix draft package and approved creative separately alongside Vista progress and the roadmap. Do not splice products into a fictional same-source run or mark a missing output complete.

Cut optional actors, alternate renderers, extra campaign variants and cosmetic polish first. Preserve source support, captions, actual media, human feedback and recording. A stuck job does not justify repeated paid generation without reconciliation.

## Ralph execution contract

Use Matt's compressed demo lane with one contract, one active task and an independent verifier. Existing content/media skills run directly through the local harness and retain their actual route. Purchased AgentsKit prose stays outside the handoff.

Reuse current copy, media, calendar and recording issues. At dispatch, Forge marks exactly one active task ready and records its D-ticket. The hosted runtime parent stays deferred to Phase B. A missing human decision is not a coding task an agent can close.

Start with the [local Ralph prompt](2026-09-09-vista-ralph-prompt.md). The [hosted plan](2026-09-09-vista-operational-loop.md) retains the longer-term architecture and audit recovery work.
