# Ralph prompt for tomorrow's local Vista demo

Prepared prompt. It does not start a loop by itself.

```text
Execute Phase A of Launch Factory for the September 9 presentation.

Read AGENTS.md, the full SDS audit at the selected candidate,
docs/superpowers/plans/2026-09-09-vista-demo-local-loop.md and the
source table in the linked hosted design. Start from audit candidate
627c93fb23ae3a1ecd1cdb979396c8d23459b295 or a reviewed descendant.
Use an isolated worktree and carry the approved plan into it. Forge
owns push/issue changes; Gabe owns PR merges.

Reggie accepts LOCAL execution for this test and asks for low-production
human/UI footage editing. The call remains tomorrow. A Replit/Base44
app, auth service, Postgres runtime and new UGC actors are not required
for this deadline. They must not block the local demonstration.

Primary case: Vista Work linked tasks from the public sources in the
plan. Tix is a backup and separate creative example. No private Vista
footage/folder was supplied. Do not wait for it, invent it or rename
Tix outputs to fill missing Vista slots.

Follow D00-D05 in order. Prepare raw screen recording before D01.
One task is active at a time. A fresh builder owns a narrow file scope
where code is needed; a different context verifies. Existing specialists
may run directly in the local harness, with the actual route recorded.

For each task:
- Check branch, edits, run ID, source/voice digest and remaining allowance
  under the existing TOTAL $500 ceiling. Preserve concurrent work.
- Read the task, its protocol and checks. Execute the smallest step.
- Retain actual source, tool/agent receipts and output bytes. Record a
  local draft as local-harness execution, without invented API IDs or bills.
- Run focused checks. A non-author inspects the actual artifact.
- Report CHANGED and VERIFIED separately. Model recommendations never
  become human approvals.
- At a human gate, show the actual artifact and pause its dependent path.
  Continue only from a real response or a prior exact approval.
- Forge commits reviewed work and records the next task and evidence.

Generate the blog, five emails, changelog, popup, social video, login
animation and campaign from one captured source pack. Select the existing
Barry email seed as one attributed expression reference. Customer actions
differ from lead actions. Omit unsupported performance/tool-count figures
and conflicting plan-entitlement claims.

Use simple media: supplied footage when available, or the declared
documentary/public-imagery walkthrough. Basic cuts, accurate burned
captions and clear sound come first. Prefer the validated MCP/skill route.
ElevenLabs can supply appropriate narration; Gabe's clone is only Gabe.
Do not regenerate optional actors or rebuild ChatCut.

Inspect the final encoded social file with sound at mobile size. Check
two muted login-animation loops plus a still/reduced-motion fallback.
Show every new video to Gabe. Preserve the accepted Tix masters.
Reggie's brief contains no numeric video duration cap.

Take one REAL revision request. Change only that asset and dependent
placements. Keep the source, versions, comments and review state beside
the actual output. Browser notes remain intent. Record Gabe's local
decisions as Gabe with exact subject evidence; Barry may remain pending
in the review package. Nothing sends or publishes.

Keep RUN.md/run.json, sources/claims/voice/requests, events/jobs/costs,
all artifacts, reviews, export manifest and recording hash. On interruption,
inspect stored receipts and provider status before resuming. Unknown
submission means reconcile, not pay again.

Deadline completion needs the observed local run, all six categories
including five emails, campaign, actual revision, local continuation and
full-run recording. Save raw capture before the shorter presentation cut
and mark time jumps. A product film alone is not the recording.

Keep a final 90-minute review/rehearsal window before the confirmed call.
If the Vista run is incomplete, show its exact progress and existing Tix
examples separately. Never fake completion with prebuilt content or a
nonfunctional chat facade.

Report local-demo completion, actual content decisions and unfinished
work separately. Hosted non-engineer use, operational service identity,
durable background recovery and recipient-cloud tests remain Phase B.
Do not start Phase B inside this deadline loop.
```

Execution plan: [Phase A](2026-09-09-vista-demo-local-loop.md). Future service plan: [Phase B](2026-09-09-vista-operational-loop.md), with its separate [hosted prompt](2026-09-09-vista-hosted-ralph-prompt.md).
