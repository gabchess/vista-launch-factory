# Resume Launch Factory

Current continuation: N01 has a [checked baseline](../packages/camp_tix_launch_001/00_baseline/baseline.json). Read its [writing gate](../packages/camp_tix_launch_001/00_baseline/CLAIMS-LOCK-REVIEW.md) and actual decision record before N02 drafts the blog. Gabe resumed without manual compaction. The earlier N01 preparation instructions below remain background for verifying that packet; do not rebuild it unnecessarily.

Read `AGENTS.md`, `docs/CHECKPOINT.md`, `docs/NEXT-STEPS.md`, `task_plan.md` and `progress.md` from the full repository. Verify current branch, working tree, remote commit and open Issues. Reconcile the plan's next ticket through Forge before a Ralph run; keep one ready execution ticket at a time.

Use the prepared baseline for Tix internal validation, then the selected Prospector source for the client demonstration. Preserve both approved Tix media files. Do not regenerate them or reopen their settled story/voice decisions merely because the context was compacted.

Finish the four remaining categories: blog, five email variants as one category, changelog, and popup graphic/copy. Each needs actual source-linked content, human review and a reusable prompt/adapter. Add the coherent campaign and weekly calendar. The 55-second Tix film still needs a separate shorter social adaptation if used for the capped client slot; the square animation still needs login-surface packaging.

The existing n8n component is preparation only. Do not claim that it renders, persists authenticated approval or drives a working app. After the content method is validated, implement the worker, durable campaign/approval state and complete orchestration. Then connect the selected Base44 or Replit app and test a full operator journey without a coding console or hidden engineer intervention.

Every actual product claim needs source evidence. Keep brand voice separate from facts. Retain true provider/source lineage. No current account credentials, paid-kit material, private messages or production media should enter the public augment by accident. Nothing auto-publishes in v1.

## Suggested Skills

- `launch-factory` and the stage-selected role for each deliverable.
- Matt Skills Curated: `to-spec`, `to-tickets`, then `implement-spec` for the selected implementation ticket.
- `agentskit` to select one installed specialist skill for a specific build task; use `planning-with-files` for the current plan. Purchased kit content stays local.
- `product-heygen-pipeline` with recipe `ugc-app-reveal` only when a new video task is reached; keep actual script, spend and human-review authority attached to that scope.

## Exact next action

Read the existing N01 baseline, its checks and the actual Claims Lock state. If the decision is still pending, present its exact writing brief for Gabe's review. If an applicable decision has arrived, have Forge select the N02 execution phase and use the prepared blog request. Preserve both approved videos and proceed only through the selected ready ticket.
